<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codingproject.spotter">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Spotter">
        <activity android:name=".FaqActivity"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".ChooseMuscleActivity"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".AddExercise"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".Exercise"
            android:parentActivityName=".ChooseMuscleActivity"/>
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>